import { useEffect, useState } from 'react';
import Team from '../../Components/Team/Teams';
import { getTeams } from '../../services/TeamRoute';

export default function TeamsList() {
  return <h1>Names of Teams</h1>;
}
